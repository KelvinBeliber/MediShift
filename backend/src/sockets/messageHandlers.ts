import { Server as SocketIOServer, Socket } from 'socket.io';

// Real-time messaging event handlers are registered here (see Phase 12: messaging).
export function registerMessageHandlers(_io: SocketIOServer, _socket: Socket): void {
  // populated in the messaging module
}
